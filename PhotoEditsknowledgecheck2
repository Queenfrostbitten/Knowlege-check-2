
using System;
using System.Collections.Generic;
using System.Text;
using System.Threading.Tasks;


namespace PhotoOrganization
{ 


	public class Image
    

	{

		public String Type { get; set; }
		public String Location { get; set; }
		public String Model { get; set; }
		public String Yearphotographed { get; set; }


		Console.WriteLine( "How many photos do you want to add? " );
var numberOfImages = int.Parse(Console.ReadLine());

		var ImageList = new List<PhotoImages>();
for (int i = 0; i<numberOfImages; i++)
{
	// In this loop, populate the object's properties using Console.ReadLine()
	var Image = new PhotoImages();
		var entry = Image.entry();
		
	Image = Console.ReadLine();
    

	ImageList.Add(PhotoImages);

 ImageList.Add( new Image() { Type = entry.Type, Location = entry.Location, Model = entry.Model, Yearphotographed = entry.Yearphotographed };


// Print out the list of records using Console.WriteLine()
foreach(var entry in ImageList)

        Console.WriteLine( "Type:" +  entry.Type);
		Console.WriteLine( "Location" + entry.Location );
		Console.WriteLine( "Model" + entry.Modelname );
		Console.WriteLine( "Yearphotographed" + entry.Yearphotographed);
}
}